//! Same struct name as config_b.rs, completely different fields — a name
//! collision, never an "EXACT twin".

pub struct QuickWinReport {
    pub action_label: String,
    pub target_file: String,
}
