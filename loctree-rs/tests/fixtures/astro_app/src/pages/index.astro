---
import Card from "../components/Card.astro";

export interface Props {
  title: string;
}

const { title = "Astro fixture" } = Astro.props;
const items = ["frontmatter", "imports", "exports"];
---

<html lang="en">
  <body>
    <Card title={title} items={items} />
  </body>
</html>
