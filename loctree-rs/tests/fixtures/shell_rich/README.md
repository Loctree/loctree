# Shell Rich Fixture

Source: copied from `loctree-suite/scripts/` and `install.sh` at commit `<HEAD-at-T4-start>`
Purpose: regression baseline for shell semantic analyzer

DO NOT auto-update this fixture from the live tree without re-running the benchmark (we are deliberately freezing the noise baseline).