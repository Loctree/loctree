import Foundation

final class TypeReferenceProbe {
    private let state: AppState
    private let url: URL

    func open(_ text: String) -> DocumentRef {
        let broken: TotallyMadeUpType
        return text as! DocumentRef
    }
}
