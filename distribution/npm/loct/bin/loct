#!/usr/bin/env node

// `loct` is the canonical CLI name. It must run the `loct` native binary, NOT
// the `loctree` one: the `loctree` binary prints its own deprecation banner,
// so routing `loct` there would nag users who already type the right name.
require('../index.js').runBinary('loct', process.argv.slice(2));
